# ── 服务器地址默认值（渲染层环境变量模板） ──────────────────────────────
# 复制为 .env（或 .env.development / .env.production）后按需修改。
#
# 优先级：UI 设置页保存的地址 > 本环境变量 > 本地回环硬编码默认。
# 「恢复默认」（设置页）会清除 UI 覆盖，重新跟随这里的值。
#
# 本地调试：无需任何配置，默认即为本地回环地址。
# 部署到阿里云等公网环境时，构建前设置（或写入 .env.production，不入库）：
#   VITE_ACCOUNT_BASE=https://account.example.com
#   VITE_CHAT_BASE=https://chat.example.com
#   VITE_WS_BASE=wss://chat.example.com/ws
#
# 也可直接通过 shell 环境变量构建：
#   VITE_CHAT_BASE=https://chat.example.com pnpm build

VITE_ACCOUNT_BASE=http://127.0.0.1:8001
VITE_CHAT_BASE=http://127.0.0.1:8002
VITE_WS_BASE=ws://127.0.0.1:8002/ws
