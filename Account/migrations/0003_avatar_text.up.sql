-- ============================================================================
-- account 库 · 头像放宽为 text
-- ----------------------------------------------------------------------------
-- 客户端没有文件上传通道，头像以 base64 data URL 形式内联在 avatar_url
-- 字段落库（渲染层已压缩到 144px JPEG，约 10KB）。VARCHAR(512) 放不下，
-- 放宽为 text。PostgreSQL 的 text 与 varchar 无性能差异。
-- ============================================================================

ALTER TABLE account_user
    ALTER COLUMN avatar_url TYPE text;
